
[project]
name = "q-haderslev-vbo"
version = "0.1.0"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["q_haderslev_vbo"]

[tool.setuptools.package-data]
q_haderslev_vbo = ["**/*.py"]
